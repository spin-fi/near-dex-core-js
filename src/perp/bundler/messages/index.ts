export const selectorErrorMessage = () => {
  return `[core:bundler:perp]: "selector" not provided`;
};

export const contractIdErrorMessage = () => {
  return `[core:bundler:perp]: "contractId" not provided`;
};
