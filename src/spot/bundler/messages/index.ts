export const selectorErrorMessage = () => {
  return `[core:bundler:spot]: "selector" not provided`;
};

export const contractIdErrorMessage = () => {
  return `[core:bundler:spot]: "contractId" not provided`;
};
